# Ask the user to type their name and store it in the variable "name"
name = input("What is your name? ")

# Create a friendly greeting message using the value the user entered
greeting = f"Hello, {name}! Nice to meet you."

# Display the greeting in the terminal so the user can see it
print(greeting)


